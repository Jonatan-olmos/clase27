// todo: hace ek front de la calculadora 
const process = require ('process');
const {argv} = process;

function calculadora (comando ) {
    
switch (comando ) {
    case "sumnar ":
        
        break;

    default:
        return "comando no valido"
        break;
}
}


console.log(calculadora(argv[2]
, argv[3]));